import { ProductionReadiness } from "@/components/production-readiness";
import { FeatureBadge, StudioPlatformPage } from "@/components/studio-platform";
import { featureFlags, platformConfig } from "@/data/platform.config";
export default function Page() { return <StudioPlatformPage active="settings" eyebrow="Configuration without secrets" title="Settings" description="Review business features and provider readiness. Secrets remain in environment variables and are never rendered here."><section className="settings-grid"><article><h2>Features</h2>{Object.entries(featureFlags).map(([key, enabled]) => <FeatureBadge key={key} enabled={enabled}>{key}</FeatureBadge>)}</article><article><h2>Providers</h2>{Object.entries(platformConfig.providers).map(([key, state]) => <div className="provider-row" key={key}><strong>{key}</strong><span>{state}</span></div>)}</article></section><ProductionReadiness/></StudioPlatformPage>; }
