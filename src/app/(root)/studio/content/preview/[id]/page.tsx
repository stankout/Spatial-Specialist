import {notFound} from "next/navigation";import {PublicContentDetail} from "@/components/public-content";import {getStudioAccess} from "@/lib/media/security";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{locale?:string}>}){if(!getStudioAccess().enabled)notFound();const [{id},{locale}]=await Promise.all([params,searchParams]);return <PublicContentDetail locale={locale==="vi"?"vi":"en"} previewId={id} allowedTypes={["video","article","guide","property-analysis","inspection-education","spatial-analysis"]}/>}
