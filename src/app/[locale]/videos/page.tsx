import {notFound} from "next/navigation";import {PublicContentIndex} from "@/components/public-content";import {VisualPage} from "@/components/visual-page";import {isLocale} from "@/lib/i18n";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const [{locale},query]=await Promise.all([params,searchParams]);if(!isLocale(locale))notFound();return <VisualPage page="videos" locale={locale} preview={query.visualPreview==="1"}><PublicContentIndex locale={locale} type="videos"/></VisualPage>}
