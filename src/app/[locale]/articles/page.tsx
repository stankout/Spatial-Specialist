import {notFound} from "next/navigation";import {PublicContentIndex} from "@/components/public-content";import {isLocale} from "@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <PublicContentIndex locale={locale} type="articles"/>}
